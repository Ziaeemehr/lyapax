# lyapax

JAX-native Lyapunov exponent computation for ODEs and DDEs.

See `notes/milestones.md` for the roadmap and `notes/validation_systems.md`
for the correctness tests this package is held to. Runnable demos live in
`examples/` (sphinx-gallery format; docs are not wired up yet).
